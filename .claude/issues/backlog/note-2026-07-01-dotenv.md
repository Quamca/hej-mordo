# .env dla kluczy API

Zamiast ustawiać GEMINI_API_KEY w każdym terminalu ręcznie — obsługa pliku `.env` w brain/.
Jeden raz wpisujesz klucz, potem `python pc_mordo.py` bez żadnego eksportu.

Biblioteka: `python-dotenv` (pip install python-dotenv).
